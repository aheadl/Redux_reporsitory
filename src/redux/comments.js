//import { COMMENTS } from '../shared/comments';
import * as ActionTypes from './ActionTypes';

//Updates added in 3. Exercise: Fetch from Server 
// ????????????? DESCRIPTION OF CHANGE FOR THIS REDUCER FILE ????????????????????????????????????????????????????????????????
//????  change the comments and promotions reducer files to no longer use the data from the files in the 
//????   shared folder, but to use the data that's fetched from the server and dispatched to the reducer as 
//????   action payloads instead. 
//????    it is no longer a simple array, but an object that contains the comments or promotions array as a property, 
//????    plus other properties (errMess for comments, errMess and isLoading for promotions).
//??????????????????????????????????????????????????????????????????????????????????????????????????????

export const Comments = (state = { errMess: null, comments: []}, action) => {
    switch (action.type) {
        case ActionTypes.ADD_COMMENTS:
            return {...state, errMess: null, comments: action.payload};

        case ActionTypes.COMMENTS_FAILED:
            return {...state, errMess: action.payload};

        case ActionTypes.ADD_COMMENT:
            const comment = action.payload;
            // comment.id = state.comments.length;
            // comment.date = new Date().toISOString();
            return {...state, comments: state.comments.concat(comment)};

        default:
            return state;
    }
};



// export const Comments = (state = COMMENTS, action) => {
//     switch (action.type) {
//         case ActionTypes.ADD_COMMENT:
//             const comment = action.payload;
//             comment.id = state.length;
//             comment.date = new Date().toISOString();
//             return state.concat(comment);
//         default:
//             return state;
//     }
// };